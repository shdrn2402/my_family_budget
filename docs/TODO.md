# 📌 Текущие мелкие задачи (TODO)

## 1. Пакетная загрузка выписок (Debounce)
**Цель:** При отправке нескольких файлов выписок (пакетом/альбомом) бот должен группировать их и присылать один сводный отчет вместо спама сообщениями на каждый файл.

- [ ] Реализовать сборщик файлов (debounce) в обработчике документов (`bot/handlers/document.py`) с задержкой ~1.5–2 секунды.
- [ ] Группировать файлы и обрабатывать их за один проход.
- [ ] Выводить один красивый сводный отчет:
  - Общее количество найденных строк
  - Количество добавленных новых транзакций
  - Количество пропущенных дубликатов
- [ ] Выводить имена файлов и конкретные ошибки только для тех выписок, при обработке которых возникли проблемы.
- [ ] Обновить и дополнить тесты для проверки пакетного импорта.

---

## 2. Учет наличных и Bit-кошельков (Сверка)
**Цель:** Реализовать корректный учет транзитных операций и наличных трат.

- [ ] Добавить алиасы для транзитного счета (например, `bit`, `paybox`) в таблицу `account_aliases` базы данных.
- [ ] Переписать логику для операции «Снятие наличных» (категория 43): теперь это должен быть внутренний перевод (трансфер) со счета карты/банка на счет `Shared Cash` («Общий сейф»), а не просто расход.
- [ ] Добавить базовое отслеживание баланса кошелька наличных.
- [ ] Добавить соотнесение переводов Bit с транзитного счета на личные счета.
